module.exports = (client, message, queue) => {

    message.channel.send(`${client.emotes.error} - A musica parou por que eu fui desconectado do server`);
            console.log("Para suporte va em bit.ly/SupportFireMusicBot")
};
